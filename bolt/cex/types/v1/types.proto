syntax = "proto3";

package bolt.cex.types.v1;

// Order type
enum MakerTakerSide {
  // Unspecified - should never be used
  MAKER_TAKER_SIDE_UNSPECIFIED = 0;
  // A maker order
  MAKER_TAKER_SIDE_MAKER = 1;
  // A taker order
  MAKER_TAKER_SIDE_TAKER = 2;
}

// Journal type
enum JournalType {
  // Unspecified - should never be used
  JOURNAL_TYPE_UNSPECIFIED = 0;
  // Trading type
  JOURNAL_TYPE_TRADING = 1;
}
