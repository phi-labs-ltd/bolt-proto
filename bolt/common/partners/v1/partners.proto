syntax = "proto3";

package bolt.common.partners.v1;

// Enum defining various partners
enum Partner {
  // Unspecified partner
  PARTNERS_UNSPECIFIED = 0;
  // Phi Labs Market Maker
  PARTNERS_MARKET_MAKER = 1;
  // Bolt UI provided by Phi Labs
  PARTNERS_BOLT_UI = 2;
  // FlowX partner integration
  PARTNERS_FLOWX = 3;
}
